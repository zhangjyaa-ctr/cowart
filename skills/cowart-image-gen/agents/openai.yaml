interface:
  display_name: "Cowart Image Gen"
  short_description: "Replace a selected Cowart AI holder with a generated image"
  default_prompt: "Use $cowart:cowart-image-gen to replace the selected Cowart AI image holder with a generated image."

policy:
  allow_implicit_invocation: true
